<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.liepajatourguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClassicsActivity"
            android:label="@string/category_classics" />
        <activity
            android:name=".ActiveLeisureActivity"
            android:label="@string/category_active_leisure" />
        <activity
            android:name=".NatureActivity"
            android:label="@string/category_nature" />
        <activity
            android:name=".DiningActivity"
            android:label="@string/category_dining" />
        <activity
            android:name=".HotelsActivity"
            android:label="@string/category_hotels" />
    </application>

</manifest>